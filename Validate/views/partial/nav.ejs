<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container d-flex flex-row">
    <a href="/" class="navbar-brand justify-content-start">Christian Wright</a>

    <div class="justify-content-end" id="navbarNav">
      <ul
        class="d-flex flex-row justify-content-between navbar-nav me-auto mb-2 mb-lg-0"
      >
        <li class="nav-item">
          <% if (pagename === "Home") { %>
          <a
            href="/"
            style="color: orange"
            id="index.html"
            class="nav-link mr-2"
            >Home</a
          >
          <% } else { %>
          <a href="/" id="index.html" class="nav-link">Home</a>
          <% } %>
        </li>

        <li class="nav-item">
          <% if (pagename === "About") { %>
          <a href="/About" style="color: orange" class="nav-link">About</a>
          <% } else { %>
          <a href="/About" class="nav-link">About</a>
          <% } %>
        </li>

        <% if (typeof(sess) === "undefined" || sess.loggedIn !== true) { %>
        <li class="nav-item">
          <% if (pagename === "Login") { %>
          <a href="/login" style="color: orange" class="nav-link">Login</a>
          <% } else { %>
          <a href="/login" class="nav-link">Login</a>
          <% } %>
        </li>
        <li class="nav-item">
          <% if (pagename === "Reg") { %>
          <a href="/reg" style="color: orange" class="nav-link">Sign Up</a>
          <% } else { %>
          <a href="/reg" class="nav-link">Sign Up</a>
          <% } %>
        </li>
        <% } else { %>
        <!-- <li class="nav-item">
          <% if (pagename === "Profile") { %>
          <a
            href="/profile?sess=<%= JSON.stringify(sess) %>"
            style="color: orange"
            class="nav-link"
            >Profile</a
          >
          <% } else { %>
          <a href="/profile?sess=<%= JSON.stringify(sess) %>` class="nav-link"
            >Profile</a
          >
          <% } %>
        </li> -->
        <li class="nav-item">
          <a href="/logout" class="nav-link">Logout</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
